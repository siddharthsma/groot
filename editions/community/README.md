# Community Edition

Single-tenant self-hosted edition with tenant-management restrictions and local
distribution packaging.

Official Community builds are expected to embed `BuildEdition=community`.
Runtime `.env` may narrow behavior, but it must not elevate the edition beyond
that build-time trust boundary.
