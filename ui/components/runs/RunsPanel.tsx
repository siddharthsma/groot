import { PlaceholderPanel } from "@/components/layout/PlaceholderPanel";

export function RunsPanel() {
  return (
    <PlaceholderPanel
      title="Run monitor placeholder"
      description="This route will later host workflow runs, delivery traces, and runtime debugging surfaces."
      eyebrow="Monitor"
    />
  );
}
