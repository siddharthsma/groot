import { DataTableScaffold } from "@/components/tables/DataTableScaffold";

export function AgentsPanel() {
  return (
    <DataTableScaffold
      title="Agent Workspace Placeholder"
      description="This route will host agent definitions, sessions, and runtime state once the UI phases start layering real data."
    />
  );
}
