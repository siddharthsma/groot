import { DataTableScaffold } from "@/components/tables/DataTableScaffold";

export function ProvidersPanel() {
  return (
    <DataTableScaffold
      title="Provider Catalog Placeholder"
      description="This route is reserved for provider discovery, configuration forms, installed plugin visibility, and operation or schema introspection."
    />
  );
}
