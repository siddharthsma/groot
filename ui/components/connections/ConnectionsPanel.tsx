import { DataTableScaffold } from "@/components/tables/DataTableScaffold";

export function ConnectionsPanel() {
  return (
    <DataTableScaffold
      title="Connections Placeholder"
      description="This route is reserved for tenant and global connection management, connection configuration, and installed integration instance visibility."
    />
  );
}
