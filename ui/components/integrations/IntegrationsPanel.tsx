import { DataTableScaffold } from "@/components/tables/DataTableScaffold";

export function IntegrationsPanel() {
  return (
    <DataTableScaffold
      title="Integration Catalog Placeholder"
      description="This route is reserved for integration discovery, configuration forms, installed plugin visibility, and operation or schema introspection."
    />
  );
}
